meta {
  name: Patch profile settings
  type: http
  seq: 3
}

patch {
  url: {{ProfileBaseAddress}}/api/v1/users/current/profilesettings
  body: json
  auth: inherit
}

body:json {
  {
      "language": "nn",
      "doNotPromptForParty": true,
      "preselectedPartyUuid": null
    }
}

script:pre-request {
  await bru.runRequest("TokenGenerator/portal user")
  
}
