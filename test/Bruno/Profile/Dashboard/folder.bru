meta {
  name: Dashboard
  seq: 1
}

vars:pre-request {
  emailAddress: {{process.env.EMAIL_ADDRESS_TEST}}
  phoneNumber: {{process.env.PHONE_NUMBER_TEST}}
  countryCode: {{process.env.COUNTRY_CODE_TEST}}
}

docs {
  # Dashboard API Tests
  
  This folder contains tests for the Support Dashboard endpoints.
  
  ## Environment Variables Required
  
  Set the following environment variables in your `.env` file:
  
  ```
  EMAIL_ADDRESS_TEST=your-test-email@example.com
  PHONE_NUMBER_TEST=99999999
  COUNTRY_CODE_TEST=%2B47
  ```
  
  - **EMAIL_ADDRESS_TEST**: Email address for notification lookup tests
  - **PHONE_NUMBER_TEST**: Phone number used in phone number search tests
  - **COUNTRY_CODE_TEST**: Country code for phone number testing (URL-encoded, e.g., `%2B47` for +47)
}
